/**
 * @deprecated Prefer getPublishedBlogs() from app/lib/data/blogs.
 * Kept briefly so any leftover sync imports fail loudly if misused.
 * Public pages and admin now use the shared data-access layer.
 */
export const blogs = [];
